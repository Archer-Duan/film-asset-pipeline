# 不要把真实密钥提交到 Git，也不要通过聊天或截图发送。
ARK_API_KEY=
# 腾讯云 API 3.0 / SDK 路径（当前默认）。这是密钥对，不是 sk- 开头的 API Key。
TENCENTCLOUD_SECRET_ID=
TENCENTCLOUD_SECRET_KEY=
# OpenAI 兼容路径保留用于兼容和诊断。
HUNYUAN_3D_API_KEY=
# 兼容旧配置名；二选一即可：TENCENT_TOKENHUB_API_KEY=
